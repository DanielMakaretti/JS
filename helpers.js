const helpers = {
    subtract: function (a, b) {
        return a - b;
      },
      add: function (a, b){
        return a + b;
      },
      p: 5544
}

export const subtract = helpers.subtract;
export const add = helpers.add;
export const p = helpers.p;